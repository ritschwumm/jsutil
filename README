Some utilities I found useful to have around

To build this code, get and install SBT from
    https://github.com/sbt/sbt

Use these commands to build:
    git clone git@github.com:ritschwumm/jsutil.git
    cd jsutil
    sbt publish-local
